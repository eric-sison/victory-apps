DATABASE_URL=postgres://appuser:password@localhost:5432/appdb

BETTER_AUTH_URL=http://localhost:3001/api/auth
BETTER_AUTH_SECRET=xozbAcrXImYXDrfljLZhsydCNuRee5Ti

# Add allowed origins - comma-separated
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:2000

# SMTP — in dev this points to Mailpit (docker compose up)
# In production, replace with your real SMTP provider credentials
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=dev
SMTP_PASSWORD=dev
SMTP_FROM=noreply@example.com

RESET_PASSWORD_CALLBACK=http://localhost:3000/auth/callback/reset-password