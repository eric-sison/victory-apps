POSTGRES_USER=appuser
POSTGRES_PASSWORD=password
POSTGRES_DB=appdb
DATABASE_URL=postgresql://appuser:password@postgres:5432/appdb

PGADMIN_DEFAULT_EMAIL=admin@gmail.com
PGADMIN_DEFAULT_PASSWORD=use_a_super_strong_password

BETTER_AUTH_SECRET=JP39xhzfVePXwtxUP/WxC5BfBtfxTuwd5gZOisW3Hu8=
BETTER_AUTH_URL=http://localhost:3001/api/auth
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3001/api/auth

NODE_ENV=production
PORT=3001
ALLOWED_ORIGINS=http://localhost:3000

DEFAULT_ADMIN_EMAIL=admin@gmail.com
DEFAULT_ADMIN_PASSWORD=use_a_super_strong_password
DEFAULT_ADMIN_NAME=Super Admin

# Frontend — used to construct links in emails (password reset, etc.)
RESET_PASSWORD_CALLBACK=https://yourdomain.com/auth/reset-password

# SMTP — replace with your real provider credentials in production
SMTP_HOST=smtp.yourmailprovider.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_user
SMTP_PASSWORD=your_smtp_password
SMTP_FROM=noreply@yourdomain.com